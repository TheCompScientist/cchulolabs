import { RouterLinkHotfixPipe } from './router-link-hotfix.pipe';

describe('RouterLinkHotfixPipe', () => {
  it('create an instance', () => {
    const pipe = new RouterLinkHotfixPipe();
    expect(pipe).toBeTruthy();
  });
});
